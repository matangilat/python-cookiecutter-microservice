# Optional dependencies to run src/utils/health_check_cli.py
# Install into your service image or create a small sidecar image that contains
# these packages if you prefer to keep the application image minimal.

asyncpg>=0.27.0
aiomysql>=0.1.0
motor>=4.3.0
redis>=4.5.0
aiokafka>=0.8.0
aio-pika>=8.1.0
aioboto3>=11.0.0
